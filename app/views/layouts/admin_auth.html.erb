<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Aurora Admin — Sign in</title>
    <%= csrf_meta_tags %>
    <%= render "admin/styles" %>
  </head>
  <body class="auth-body">
    <main><%= yield %></main>
  </body>
</html>
